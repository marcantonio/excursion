services:
  excursion-remote:
    image: excursion-remote
    build: .
    container_name: excursion-remote-test-env
    ports:
      - "2222:22"
    volumes:
      - ./remote_home:/home/emacsuser
      - ./emacsuser_key.pub:/tmp/emacsuser_key.pub:ro
    environment:
      - BIN_NAME
    tty: true
